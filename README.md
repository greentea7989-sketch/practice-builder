# Practice Builder

A lightweight HTML/CSS/JS project for building and iterating on UI layouts.

## Files

- `index.html`: Markup and structure
- `style.css`: Styles
- `main.js`: Client-side behavior
- `blueprint.md`: Design/feature notes

## Run

Open `index.html` directly in a browser, or serve it with any static server.
